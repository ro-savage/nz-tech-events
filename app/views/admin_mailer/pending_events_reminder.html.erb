<div style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;">
  <p><%= pluralize(@pending_events_count, "pending event") %> <%= @pending_events_count == 1 ? "needs" : "need" %> approval.</p>

  <p>Review <%= link_to "the pending events page", @pending_events_url %>.</p>
</div>
